import React, { FC } from 'react';
import { Container } from '../../lib/styled/container';

const Projects: FC = () => {
  return (
    <section>
      <Container>
        <p>Projects</p>
      </Container>
    </section>
  );
};

export default Projects;
