import React, { FC } from 'react';
import { Container } from '../../lib/styled/container';
import { Section } from './style';

const Hero: FC = () => {
  return (
    <Section>
      <Container>
        <p>Hero</p>
      </Container>
    </Section>
  );
};

export default Hero;
