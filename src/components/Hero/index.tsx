import React, { FC } from 'react';
import { Container } from '../../lib/styled/container';

const Hero: FC = () => {
  return (
    <section>
      <Container>
        <p>Hero</p>
      </Container>
    </section>
  );
};

export default Hero;
