import React, { FC } from 'react';
import { Container } from '../../lib/styled/container';
import { Section, Wrapper } from './style';

const ForgetPasswordForm: FC = () => {
  return (
    <Section>
      <Container>
        <Wrapper>ForgetPasswordForm</Wrapper>
      </Container>
    </Section>
  );
};

export default ForgetPasswordForm;
